package com.esiea.monstre.poche.models.battle.logs.enums;

public enum LoggingMode {
    LOCAL_ONLY,      // Logs uniquement sur le terminal local
    NETWORK_ONLY,    // Logs uniquement vers la connexion réseau
    BOTH             // Logs à la fois en local et sur le réseau
}
